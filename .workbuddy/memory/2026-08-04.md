# 2026-08-04 工作日志

## 图片改色工具 ColorForge

### 任务
用户要求从 GitHub 获取作图相关模型与技能，构建图片改色工具（改特定颜色 / 改全部颜色）。

### 执行过程
1. **搜索 GitHub**：找到两个核心项目：
   - `watermelonPop/replace-color`：基于 Canvas 的点击取色+容差匹配替换
   - `nkzw-tech/palette-swap`：调色板交换+色相偏移，TypeScript，性能优化（Uint32Array）
2. **克隆分析**：两个仓库均克隆到本地，分析其核心算法
3. **构建工具**：基于两者技术方案，融合构建 `colorforge.html`（单文件，无外部依赖）

### 工具功能
- **模式1 - 特定颜色替换**：点击画布取色 / 手动选色 → 设目标色 → 容差匹配 → 边缘柔化选项
- **模式2 - 全局色调调整**：色相偏移(-180~180°) + 饱和度 + 亮度，实时预览
- **模式3 - 全部改色**：保留明暗(基于HSL亮度重映射) 或 纯色填充，可调强度
- 主色调自动提取（量化采样+排序）
- 拖拽上传 / 重置 / 下载PNG

### 技术要点
- RGB 欧氏距离平方做容差匹配
- HSL 色彩空间做色调调整
- smoothstep 做边缘柔化
- 32级量化做主色提取
- 采样优化大图性能

### 文件位置
- 本地：`colorforge.html`
- 双接口共享：`\\Os-20250608fqtb\双接口\workbuddy\colorforge.html`
- 参考仓库：`replace-color/` 和 `palette-swap/`

---

## 批量改色生成器 BatchColorForge

### 需求
用户要在前一个工具基础上增加批量功能：指定一个源颜色（如背景色），一次性生成多张不同颜色替换的图片。

### 实现 `batch-colorforge.html`
- 指定源颜色（点击图片取色 / 颜色选择器 / 主色调快选）
- 目标颜色列表管理：手动添加、名称编辑、删除
- 快捷添加：彩虹7色、渐变10色、预设色板
- 容差 + 边缘柔化 + 保留透明区域选项
- 生成结果以网格展示，每张可单独下载
- **JSZip 打包下载全部 PNG**（CDN引入）
- 异步分批生成，进度提示

## 批量改色 v2 优化（2026-08-05）

### 用户反馈
"有什么变化吗，识别图片给我加大色彩范围，选择只能一点，改变颜色一定要出现我要求的颜色"
- 上传带蓝色钻石图案的图片，默认源色 #FFFFFF 无法匹配 → 没替换
- 手动选色流程繁琐

### 改进 `batch-colorforge.html` v2
- **去掉源色手动选色框**：源色只能通过点击图片 OR 点主色板选择（"一点"）
- **自动主色板**：上传图片后立即提取 Top 12 主色 → 大色块展示 → 一键选中
- **容差扩大到 0-500**：默认 80，覆盖更大色彩范围
- **预览开关**：勾选后匹配区域实时红色高亮，所见即所得
- **像素计数**：选完源色立刻显示"匹配 N 像素"，避免生成后才发现无效
- **找不到匹配时**：自动提示提升容差到 200 重试
- **默认硬替换**：移除柔化默认开启，避免结果色被混合稀释
- **保留透明**：PNG alpha 区域不被破坏

## ColorForge Pro v3（2026-08-05）

### 需求
"识别原图色彩可以多选，生成颜色也可以加上一步加上材质效果和一些平面的常见图片，加上金色银色类的颜色，颜色可根据原选择色彩范围以外的不同三十个常见色，选的快一点智能一点"

### 实现 `colorforge-pro.html`
- **源色多选**：点图片/点色板 toggle 多个源色，同时替换多个颜色范围
- **材质效果**：8种金属材质（金/银/青铜/紫铜/玫瑰金/铬/铂金/珍珠），用亮度映射256级渐变LUT，保留立体感
- **30种常见色色板**：红5+橙黄5+绿5+蓝5+紫5+中性5，一键全加
- **Tab式选择**：常见色/材质/自定义 三个标签页
- **智能选择**：主色板自动提取Top15，点选即toggle，选中状态有✓标记
- **进度条**：生成过程显示进度
- **文件**：1192行/57KB，已同步双接口

### Git 仓库（2026-08-05）
- 初始化本地 Git 仓库，提交所有文件（30 files, 7074 lines）：
  - colorforge.html / batch-colorforge.html / colorforge-pro.html
  - replace-color/ 和 palette-swap/ 参考源码
  - .gitignore 配置
- 修复 `colorforge-pro.html` canvas 点选源色失效问题：同时监听 canvas 与 canvas-wrap，增加点击波纹反馈
- 增加 `click-test.html` 独立诊断页，验证浏览器 canvas 点击/取色能力正常
- v3.1 重构点击处理：移除绝对定位覆盖层、inline onclick + addEventListener 双兜底、页面内 cfLog 调试面板、关键流程 try-catch
- **v3.2 全面 null-safety**：用户反馈调试日志显示 `toggleSourceColor` 在 textContent 赋值时崩溃（Cannot set properties of null）。根因为多个 `getElementById` 未判空（genCount/tgtBadge/toast/loadingText/loading/srcBadge/srcChips/srcStats/等）。已为全文件所有 getElementById 调用加 null guard，任一环节返回 null 不再崩溃。（Git: `9cd28d8`）
- **v3.3 预览强制开启**：去掉 `if (previewEnabled)` 条件，选色后始终调用 drawPreview()。（Git: `cdd51ce`）
- **v6.0 整体色调映射版（对齐参考图效果）**：用户上传截图三显示参考效果是"原图整体单色调映射"。之前 v5.0 做"源色替换"破坏纹理。v6.0 重写核心：
  - 上色模式：color（保留亮度替换色相饱和度）、hue（仅改色相）、overlay（柔光叠加）
  - HSL 亮度保留：只替换 H/S，原图 L 通道保留所有纹理/光影/边缘
  - 材质渐变映射：按原图亮度映射到金属渐变（金/银/青铜/紫铜/玫瑰金/铬/铂金/珍珠）
  - 色域识别改为保护蒙版：点选色域可保留原色，其余区域变色
  - 流程简化，预览实时响应，批量生成高保真（Git: `347206b`）
