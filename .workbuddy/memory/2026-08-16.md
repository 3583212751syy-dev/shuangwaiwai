# 2026-08-16

- ColorForge Pro 升级到 v9.0：
  - 新增「单色替换」模式：点图取源色 + 目标色，只替换所选色，保留纹理与光影（HSL 平滑映射 + 容差/羽化）。
  - 新增「全部预览」网格：全局模式显示 16 预设缩略图；单色模式显示 30 常用目标色效果，点击即应用。
  - 本地集成第三方库（`colorforge-libs/vendor/`）：JSZip、chroma.js、color-thief。
  - 上传后自动用 ColorThief 提取主色，作为源色候选。
  - 已提交 Git（commit dfc6f3b）并同步到 `\\Os-20250608fqtb\双接口\workbuddy\`。
- 沉淀可复用技能：`image-color-tool`（构建单文件图片改色工具：全局 HSL + 单色替换 + 预览网格 + 批量导出），已打包验证。
- ColorForge Pro v9.1（commit 35e679d）：
  - 把全局调色从「色相偏移」改为「目标色柔和映射」：用户选择/预设的目标色就是结果图主色。
  - 新增「保护中性」算法：纯白、纯黑、灰色几乎不被着色，保留高光/暗部/毛发细节。
  - 新增目标色选择器、着色强度、保护中性三个控件；预设主题全部带 targetHex。
  - 批量生成支持目标色参数，所有预览统一使用新算法。
  - 已同步到 `\\Os-20250608fqtb\双接口\workbuddy\`。
- ColorForge Pro v9.2（commit 356e151）：
  - 修复全局目标色映射不准确：非中性像素直接采用目标色相/饱和度，强度 100 时结果主色严格等于目标色。
  - 优化中性保护阈值：仅对极低饱和、极暗、极亮像素生效，避免淡彩色被过度保护导致目标色不纯。
  - 已同步到 `\\Os-20250608fqtb\双接口\workbuddy\`。
- ColorForge Pro v9.3（commit 83131f6）：
  - 全局预设主题从 16 个扩充到 30 个，新增猩红/珊瑚/朱橙/焦糖/姜黄/青柠/苹果绿/翡翠/孔雀/天蓝/靛蓝/薰衣草/洋红/大地棕。
  - 已同步到 `\\Os-20250608fqtb\双接口\workbuddy\`。
- ColorForge Pro v9.4（commit e3f47a8）：
  - 修复预览/缩略图模糊：主 canvas 与 thumb canvas 增加 `image-rendering: high-quality`/`crisp-edges`。
  - 缩略图按 `devicePixelRatio` 生成高像素版本，避免 CSS 100% 宽度拉伸导致马赛克。
  - 已同步到 `\\Os-20250608fqtb\双接口\workbuddy\`。
- ColorForge Pro v9.5（commit 1d1d57b）：
  - 用户对比第三方效果图后反馈质量低，调研 GitHub 高级调色库：grader-npm（3D LUT）、AlloyImage（PS 混合模式/曲线/可选颜色）、glfx.js（WebGL）。
  - 克隆参考库到 `colorforge-libs/{grader-npm,AlloyImage,glfx.js}/`，集成学习其颜色分级思路。
  - 实现「电影风格颜色分级」（cinematic grade）：暗部/亮部双端点渐变映射（shadowHex→targetHex→highlightHex）+ 保留原图亮度纹理 + 鲜艳度提升。
  - 关键算法：hueLerp 色相最短路径插值；低饱和像素直接采用映射色相（不再保留不可靠的原色相）；保护中性（白/黑/灰）。
  - 新增 UI：风格切换按钮（自然/电影）、暗部/亮部拾色器、鲜艳度滑块；27 个彩色预设主题全部升级为 cinematic 并带 vibrance 参数。
  - 单元测试验证（Node 7 像素）：gray→#10b981 系、orange/blue 均正确映射且保留亮度，无灰绿脏色。
  - 已同步到 `\\Os-20250608fqtb\双接口\workbuddy\`。
